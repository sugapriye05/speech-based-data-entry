# speech-based-data-entry
This Python script is a Voice-Activated Data Entry System that collects a user's name, email, and message via speech, then sends the data to a Google Sheet using a Google Apps Script Webhook.
